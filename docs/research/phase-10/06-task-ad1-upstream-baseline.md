# Phase 10 Addendum AD01 Task AD1 — Advanced Upstream Baseline

- **Issue:** [#201](https://github.com/OS4CSAPI/ogc-client-CSAPI_2/issues/201)
- **Status:** Acceptance evidence
- **Date:** August 28, 2026
- **Purpose:** Lock the exact upstream commit, changed-path population, and merge forecast authorized for Task AD2.

## 1. Conclusion

PASS. Canonical `upstream/main` remains exactly at the approved AD01 commit. The workbench was clean and synchronized, the 29-path inventory is unchanged, and the read-only merge forecast reports exactly the three reviewed content conflicts plus the automatic `src/index.ts` overlap.

Task AD2 is authorized to merge only `00a8931c679fb9c5cd7c5e0f0f04a4b87a307b2d`, subject to its own issue-entry checks.

## 2. Locked Repository State

| Fact | Recorded value |
| --- | --- |
| Workbench branch | `phase-10` |
| Workbench starting SHA | `bee9ec696db9ffce7ae05ebd7c15438b0d4d93bd` |
| Remote workbench SHA | `bee9ec696db9ffce7ae05ebd7c15438b0d4d93bd` |
| Delivery `clean-pr` head | `5f7cbd166143be76b60ea54593d6f313c75c3624` |
| Prior approved upstream baseline and merge base | `305e3da2cf86cfda5c3254a0be419db70cce54b0` |
| Locked AD01 upstream SHA | `00a8931c679fb9c5cd7c5e0f0f04a4b87a307b2d` |
| Current canonical `upstream/main` | `00a8931c679fb9c5cd7c5e0f0f04a4b87a307b2d` |
| Workbench-only commits | 928 |
| Upstream-only commits | 8 |
| Active merge/rebase/cherry-pick/revert/bisect | None |

Both `origin` and `upstream` fetched successfully. The fetches emitted the known non-fatal warning that Git could not delete stale metadata for `.git/worktrees/phase10-guide-merge`. Both requested refs updated successfully, and the warning did not alter the branch, worktree, inventory, or forecast.

Ancestry checks confirmed:

- the prior baseline is an ancestor of the workbench;
- the prior baseline is an ancestor of the locked AD01 upstream commit;
- the locked AD01 commit is an ancestor of current `upstream/main`; and
- current `upstream/main` is an ancestor of the locked AD01 commit.

The last two checks prove equality. Canonical upstream has not advanced beyond the approved AD01 baseline, so the upstream-advance stop rule does not apply.

## 3. Machine-Generated Upstream Inventory

The complete inventory is stored in [`06-task-ad1-upstream-changed-files.txt`](./06-task-ad1-upstream-changed-files.txt).

It was generated by:

```bash
git diff --name-status --find-renames \
  305e3da2cf86cfda5c3254a0be419db70cce54b0 \
  00a8931c679fb9c5cd7c5e0f0f04a4b87a307b2d
```

The result contains exactly 29 paths:

| Status | Count |
| --- | ---: |
| Modified | 20 |
| Added | 5 |
| Deleted | 4 |
| **Total** | **29** |

The earlier preflight, Implementation Guide Addendum, Roadmap Addendum, and issue #201 repeated an incorrect `23 modified / 3 added / 3 deleted` summary. The complete 29-path list in the preflight was correct; only its arithmetic status summary was wrong. This task corrects the summary to the mechanically reproduced `20 / 5 / 4`. The correction changes no path, conflict, scope boundary, or implementation decision.

## 4. Contribution Overlap

An exact intersection of paths changed from the prior baseline to the workbench and paths changed by PR #171 contains four entries:

1. `app/api.data.js`;
2. `src/index.ts`;
3. `src/ogc-api/endpoint.ts`; and
4. `src/ogc-api/info.ts`.

The first, third, and fourth are content conflicts. `src/index.ts` merges automatically and remains a mandatory semantic inspection point.

`src/ogc-api/model.ts` is not a textual contribution overlap. It remains the required semantic companion path because upstream moved the `allCollections` result shape into `OgcApiCollectionCapabilities`, while the delivered contribution already returns the optional `hasConnectedSystems` capability flag. Task AD2 must add only that optional field to the upstream interface as authorized by the Implementation Guide Addendum.

## 5. Merge Forecast

The read-only forecast command was:

```bash
git merge-tree --write-tree --messages \
  phase-10 \
  00a8931c679fb9c5cd7c5e0f0f04a4b87a307b2d
```

The forecast produced tree `9163826d7839760d87e7f2914cdd5fab3e8f42de` and exited `1`, the expected status for a successfully generated conflicted merge tree.

It reported exactly:

```text
Auto-merging app/api.data.js
CONFLICT (content): Merge conflict in app/api.data.js
Auto-merging src/index.ts
Auto-merging src/ogc-api/endpoint.ts
CONFLICT (content): Merge conflict in src/ogc-api/endpoint.ts
Auto-merging src/ogc-api/info.ts
CONFLICT (content): Merge conflict in src/ogc-api/info.ts
```

No other conflict or contribution-overlap path was reported. The forecast did not modify the index, working tree, branch, or remote.

## 6. Acceptance-Gate Results

| Gate | Result |
| --- | --- |
| Fetch `origin` | PASS; requested ref synchronized |
| Fetch `upstream` | PASS; current `main` resolved |
| Clean branch and origin equality | PASS; both at `bee9ec696db9ffce7ae05ebd7c15438b0d4d93bd` |
| No active Git operation | PASS; none |
| Canonical upstream disposition | PASS; current `upstream/main` equals locked AD01 SHA |
| Merge base | PASS; exact prior baseline `305e3da2cf86cfda5c3254a0be419db70cce54b0` |
| Left/right counts | PASS; 928 workbench-only and 8 upstream-only |
| Changed-path inventory | PASS; exactly 29 paths |
| Status counts | PASS after factual correction; 20 modified, 5 added, 4 deleted |
| Contribution overlaps | PASS; exact four-path population |
| Merge forecast | PASS; exact three reviewed conflicts and automatic `src/index.ts` overlap |
| Semantic companion | PASS; `src/ogc-api/model.ts` recorded for AD2 |
| Mutation audit | PASS; no merge, source edit, package install, delivery mutation, or PR change |

## 7. Task AD2 Authorization

Task AD2 may merge only `00a8931c679fb9c5cd7c5e0f0f04a4b87a307b2d` into workbench `phase-10`. It must resolve only the three recorded conflicts, apply the single authorized `src/ogc-api/model.ts` companion adaptation, inspect `src/index.ts`, and run its focused gates.

AD2 must stop if its issue-entry state, actual conflict population, or canonical-upstream disposition differs from this evidence.

Deviation from locked decision: none. Factual count correction: `23/3/3` replaced by the mechanically verified `20/5/4` without scope or path-population change.
